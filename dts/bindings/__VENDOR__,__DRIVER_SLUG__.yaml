description: __DRIVER_UPPER__ device driver

compatible: "__COMPAT__"

# FILL IN when bringing up hardware, e.g.:
#   include: i2c-device.yaml
#   on-bus: i2c

properties:
  # FILL IN: mirror fields of __DRIVER_SLUG___config_t
