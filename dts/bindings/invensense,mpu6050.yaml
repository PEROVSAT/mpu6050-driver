description: MPU6050 device driver

compatible: "invensense,mpu6050"

# FILL IN: include bus binding if the device sits on a bus, e.g.:
# include: i2c-device.yaml

properties:
  # FILL IN: devicetree properties (mirror struct mpu6050_config fields)
